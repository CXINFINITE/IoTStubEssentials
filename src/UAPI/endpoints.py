# We require 2 endpoints as shown, but should be connected to same function:

# uapi/
# uapi/<path/with/slashes/optional/>/

# Later, supply apicode, request (web request, for example), data, and path
# to UAPI.UAPIManager.fetch function.
