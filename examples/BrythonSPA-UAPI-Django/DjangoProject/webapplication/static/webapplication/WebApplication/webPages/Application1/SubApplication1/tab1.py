class Tab1:
   def entry (event=None):
      print('Tab1 called')
      # return False if not loaded properly.
      return True
   
   def exit (event=None):
      print('Tab1 exiting ...')
      # return False if condition not satisfied or so ...
      return True
